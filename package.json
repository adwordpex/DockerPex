{
  "name": "lead-scraper",
  "version": "1.0.0",
  "description": "Lead scraper using SerpAPI and Playwright",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.18.0",
    "axios": "^1.6.2",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "playwright": "^1.40.0",
    "serpapi": "^2.1.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
{
  "name": "ชื่อโปรเจกต์ของคุณ",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npx playwright install --with-deps chromium",
    "start": "node src/index.js"
  },
  "dependencies": {
    ...
  }
}
```

4. Commit & Push
5. Render จะ auto-deploy ใหม่

**แนะนำให้ใช้วิธีที่ 1 ก่อน** เพราะเร็วกว่า แค่ไปแก้ Build Command ใน Render Settings เป็น:
```
npm install && npx playwright install --with-deps chromium
